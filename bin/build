#!/bin/sh

DIR=`pwd`

if [ ! -d "OUT" ];then
    mkdir out
fi

echo $DIR
ls -al $DIR
cp node_modules/swagger-ui-dist/* $DIR/out/
curl -OL https://petstore.swagger.io/v2/swagger.json --output $DIR/out/swagger.json
sed -i "s|https://petstore.swagger.io/v2/swagger.json|swagger.json|g" $DIR/out/index.html
ls -al $DIR/out
